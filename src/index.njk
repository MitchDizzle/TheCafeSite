---
layout: layouts/base.njk
title: "The Café | Catering & Meal Prep"
description: "The Cafe – Professional catering and meal prep services. Crafted with care for every occasion."
ogTitle: "The Café | Catering & Meal Prep — Leavenworth, KS"
ogDescription: "Professional catering and meal prep services in Leavenworth, Kansas. Crafted with care for every occasion."
pageCss: "/assets/css/home.css"
# Gateway page: no nav, no footer, so the shared nav/footer JS has nothing to do.
noSiteJs: true
headExtra: |
  <!-- Local-search structured data. Only facts already published on this site —
           no hours, prices, or geo coordinates until the client confirms them
           (adding unverified ones here would contradict Google Business Profile). -->
      <script type="application/ld+json">
      {
        "@context": "https://schema.org",
        "@type": "FoodEstablishment",
        "@id": "https://lvcafetogo.com/#business",
        "name": "The Café",
        "description": "Catering and meal prep serving Leavenworth, Kansas and the surrounding area.",
        "url": "https://lvcafetogo.com/",
        "telephone": "+1-913-547-9994",
        "email": "theabcafe@gmail.com",
        "foundingDate": "2005-10",
        "address": {
          "@type": "PostalAddress",
          "streetAddress": "417 S 6th St",
          "addressLocality": "Leavenworth",
          "addressRegion": "KS",
          "postalCode": "66048",
          "addressCountry": "US"
        },
        "areaServed": "Leavenworth, Kansas and the surrounding area"
      }
      </script>
---
<div class="construction-banner" role="note">
    Pardon our crumbs — this website is still under construction. Some details may change.
</div>

<main class="hero" role="main">

    <div class="slideshow" aria-hidden="true">
        <!-- Slide 1: eager — visible on first paint. Replace placeholder div with real img tag. -->
        <div class="slide slide-1 active">
            <!-- <img src="assets/slides/slide1.jpg" alt="" loading="eager" fetchpriority="high"> -->
            <div class="slide-placeholder"></div>
        </div>
        <!-- Slides 2–4: gradient placeholders until real photos exist.
             To enable a photo, drop the file in assets/slides/ and add
             data-src="assets/slides/slideN.jpg" back onto that slide —
             the script lazy-swaps it in just before the slide shows.
             Leave data-src off while the file is missing or it 404s. -->
        <div class="slide slide-2">
            <div class="slide-placeholder"></div>
        </div>
        <div class="slide slide-3">
            <div class="slide-placeholder"></div>
        </div>
        <div class="slide slide-4">
            <div class="slide-placeholder"></div>
        </div>
    </div>

    <div class="overlay" aria-hidden="true"></div>

    <div class="hero-content">
        <p class="hero-eyebrow">Catering &amp; Meal Prep Services</p>
        {# The wordmark is Book Jacket, converted to outlines in Inkscape.

           It is NOT loaded as a webfont — the file was licensed for print via
           the sign shop, and an @font-face rule would distribute the binary to
           every visitor. Outlines ship no font file.

           inlineSvg rather than <img>: the artwork carries no fill of its own,
           so inside an <img> it would render black. Inlined, `fill` inherits
           from .hero-wordmark below and the same file can be recoloured.

           If the file goes missing the styled text below stands in. #}
        {%- set wordmark = "/assets/thecafe.svg" %}
        {%- if wordmark | hasAsset %}
        <h1 class="hero-title hero-title--art">
            {% inlineSvg wordmark, "hero-wordmark", site.name %}
        </h1>
        {%- else %}
        <h1 class="hero-title">The Café</h1>
        {%- endif %}
        <svg class="filigree-divider" viewBox="0 0 400 60" xmlns="http://www.w3.org/2000/svg">
            <path class="diamond-outer" d="M 200 10 l 15,20 -15,20 -15,-20 15,-20 Z" fill="none" stroke="var(--cream)" stroke-width="1" pathLength="1"/>
            <path class="diamond-outer" d="M 200 10 l -15,20 15,20 15,-20 -15,-20 Z" fill="none" stroke="var(--cream)" stroke-width="1" pathLength="1"/>
            <path class="diamond-inner" d="M 200 20 l 15,10 -15,10 -15,-10 15,-10 Z" fill="none" stroke="var(--cream)" stroke-width="1" pathLength="1"/>
            <path class="diamond-inner" d="M 200 20 l -15,10 15,10 15,-10 -15,-10 Z" fill="none" stroke="var(--cream)" stroke-width="1" pathLength="1"/>
            <path class="line" d="M 215 30
            c 0 0, 5 -7, 10 0 
            l 0,0 5,-7 20,7 
            3,-3 3,3 
            50,0 
            c 0 0, 5 -7, 10 0 
            l 3,-3 3,3
            c 0 0, 5 -7, 10 0 
            l 0,0 5,-5 20,5 
            l 10,0
            " fill="none" stroke="var(--cream)" stroke-width="1" transform="translate(0,0)" pathLength="1"/>
            <path class="line" d="M 215 30
            c 0 0, 5 -7, 10 0 
            l 0,0 5,-7 20,7 
            3,-3 3,3 
            50,0 
            c 0 0, 5 -7, 10 0 
            l 3,-3 3,3
            c 0 0, 5 -7, 10 0 
            l 0,0 5,-5 20,5 
            l 10,0
            " fill="none" stroke="var(--cream)" stroke-width="1" transform="scale(-1,1) translate(-400,0)" pathLength="1"/>
            <path class="line" d="M 215 30
            c 0 0, 5 -7, 10 0 
            l 0,0 5,-7 20,7 
            3,-3 3,3 
            50,0 
            c 0 0, 5 -7, 10 0 
            l 3,-3 3,3
            c 0 0, 5 -7, 10 0 
            l 0,0 5,-5 20,5 
            l 10,0
            " fill="none" stroke="var(--cream)" stroke-width="1" transform="scale(1,-1) translate(0,-60)" pathLength="1"/>
            <path class="line" d="M 215 30
            c 0 0, 5 -7, 10 0 
            l 0,0 5,-7 20,7 
            3,-3 3,3 
            50,0 
            c 0 0, 5 -7, 10 0 
            l 3,-3 3,3
            c 0 0, 5 -7, 10 0 
            l 0,0 5,-5 20,5 
            l 10,0
            " fill="none" stroke="var(--cream)" stroke-width="1" transform="scale(-1,-1) translate(-400,-60)" pathLength="1"/>
        </svg>
        <nav class="hero-actions" aria-label="Main navigation">
            <a href="contact" class="btn btn-primary">Contact & Catering</a>
            <a href="about"   class="btn btn-ghost">Learn More</a>
        </nav>
        <div class="hero-subspace">
            <p class="hero-subtitle">
                <a href="tel:+19135479994">(913) 547-9994</a><br/>
                <a href="mailto:theabcafe@gmail.com">theabcafe@gmail.com</a><br/>
                <a href="https://maps.google.com/?q=417+S+6th+St,+Leavenworth,+KS+66048">417 S 6th St, Leavenworth, KS 66048</a>
            </p>
        </div>
    </div>

    <!-- Plain buttons, not role="tab": there are no tabpanels to own, and the
         slides themselves are decorative (aria-hidden). -->
    <div class="dots" role="group" aria-label="Slideshow navigation">
        <button class="dot active" type="button" aria-label="Show slide 1" aria-current="true" data-index="0"></button>
        <button class="dot"        type="button" aria-label="Show slide 2" data-index="1"></button>
        <button class="dot"        type="button" aria-label="Show slide 3" data-index="2"></button>
        <button class="dot"        type="button" aria-label="Show slide 4" data-index="3"></button>
    </div>

</main>

<script>
(function () {
    const slides  = document.querySelectorAll('.slide');
    const dots    = document.querySelectorAll('.dot');
    let current   = 0;
    let timer;

    // 10s crossfade + 6s hold. Keep above the .slide transition duration.
    const SLIDE_INTERVAL = 16000;

    function loadSlide(slide) {
        const src = slide.dataset.src;
        if (!src) return;
        const placeholder = slide.querySelector('.slide-placeholder');
        const img = new Image();
        img.alt = '';
        img.src = src;
        img.style.cssText = 'width:100%;height:100%;object-fit:cover;display:block;';
        img.onload = () => { if (placeholder) placeholder.replaceWith(img); };
        delete slide.dataset.src;
    }

    function goTo(index) {
        slides[current].classList.remove('active');
        dots[current].classList.remove('active');
        dots[current].removeAttribute('aria-current');

        current = index;
        slides[current].classList.add('active');
        dots[current].classList.add('active');
        dots[current].setAttribute('aria-current', 'true');

        // Pre-load the next slide so it's ready
        const next = slides[(current + 1) % slides.length];
        if (next.dataset.src) loadSlide(next);
    }

    function advance() { goTo((current + 1) % slides.length); }

    function startTimer() { timer = setInterval(advance, SLIDE_INTERVAL); }
    function resetTimer()  { clearInterval(timer); startTimer(); }

    dots.forEach(dot => {
        dot.addEventListener('click', () => {
            goTo(parseInt(dot.dataset.index, 10));
            resetTimer();
        });
    });

    // Pre-load slide 2 immediately so it's ready when the timer first fires
    loadSlide(slides[1]);

    // Pause autoplay when tab is not visible
    document.addEventListener('visibilitychange', () => {
        document.hidden ? clearInterval(timer) : startTimer();
    });

    startTimer();
}());
</script>
