# share/

Handoff copies of studio artwork, committed **only** so they can be downloaded
from GitHub on a phone. Everything here is generated — the real source is the
piece in `src/studio/`, and `npm run export` rebuilds it.

These copies go stale. If one disagrees with `/studio/downloads/`, the export
is right and this is old. Re-copy or delete rather than editing anything here.
